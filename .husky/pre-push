#!/usr/bin/env sh
# Surfaces problems locally before push.
# Rebuild against local audio so Playwright and the static export use the same assets.
pnpm lint && pnpm typecheck && pnpm test && AUDIO_SOURCE=local pnpm build && pnpm test:e2e
