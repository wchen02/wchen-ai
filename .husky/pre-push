#!/usr/bin/env sh
# Same checks as CI; surfaces problems locally before push.
# In CI/automated environments these checks run as separate workflow steps.
if [ -n "$CI" ] || [ -n "$GITHUB_ACTIONS" ]; then exit 0; fi
pnpm lint && pnpm typecheck && pnpm test && pnpm test:e2e
