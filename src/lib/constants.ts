/**
 * Performance targets for critical routes (plan: LCP ≤ 2.5s, CLS ≤ 0.1).
 * Use when documenting or asserting metrics (e.g. in CI or Lighthouse config).
 */
export const PERF_LCP_TARGET_MS = 2500;
export const PERF_CLS_TARGET = 0.1;
